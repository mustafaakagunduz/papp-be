spring.application.name=pappapp
spring.profiles.active=dev

# JWT Configuration
jwt.secret=mySecretKey123456789012345678901234567890
jwt.expiration=86400000
jwt.refresh.expiration=604800000

# JPA Configuration - Hibernate'i zorla
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.hbm2ddl.auto=update
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# Hibernate schemalari otomatik olustur
spring.jpa.properties.hibernate.default_schema=public
spring.jpa.properties.hibernate.hbm2ddl.create_namespaces=true

# Email Configuration (Gmail - tum environmentlarda ayni)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=akagunduzmustafa00@gmail.com
spring.mail.password=qkni tobr slio ymsm
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Application Settings
app.name=Ilan Platformu
app.verification.code.length=6
app.verification.code.expiry=900000
app.verification.max.attempts=3
app.verification.block.time=900000

# Password Reset Configuration
app.password.reset.token.expiry=3600000

# Template Engine
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html
spring.thymeleaf.mode=HTML